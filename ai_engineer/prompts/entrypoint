Your task is to construct a bash command for running a given python project using UV.

Format:
uv pip install -r <requirements_file> (optional)
uv run <target_file> <optional arguments>

Output the command in a code block.

```
CODE
```
Note: The target file and any arguments should be based on the current project structure.